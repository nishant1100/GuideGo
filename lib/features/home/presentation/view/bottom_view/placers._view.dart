import 'package:flutter/material.dart';

class PlacersView extends StatelessWidget {
  const PlacersView ({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Places Page', style: TextStyle(fontSize: 20)),
    );
  }
}